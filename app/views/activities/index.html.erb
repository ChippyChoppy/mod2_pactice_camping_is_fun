<h1>All The Activities</h1>

<ul>
  <% @activities.each do |activity| %>
    <li><%= link_to activity.name, activity_path(activity.id) %></li>
  <% end %>
</ul>

<p><%= link_to "Activity Signup!", new_camper_activity_path %></p>
<p><%= link_to "Camper Index", campers_path %></p>